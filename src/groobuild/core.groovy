package groobuild;

import java.util.ArrayList;

/**
 * Expand meta class to define convenience methods for GrooBuild
 *
 * @author Kohsuke Kawaguchi
 */
class core {{
    // this is probably too much magic
//    /**
//     * Convert a list into a PATH format ("a:b:c" on Unix and "a;b;c" on Windows)
//     */
//    ArrayList.metaClass.toPath = { ->
//        delegate.join(File.pathSeparator)
//    }
}}